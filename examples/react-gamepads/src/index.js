import React from 'react';
import ReactDOM from 'react-dom';
import GamepadDisplay from './GamepadDisplay';

ReactDOM.render((
  <GamepadDisplay mapping={'Xbox One'} />
), document.getElementById('root'));